apiVersion: v1
kind: PersistentVolume
metadata:
  name: mysql-pv
spec:
  capacity:
    storage: 1Gi 
  hostPath:
   path: /data/mysql
   type: DirectoryOrCreate
  accessModes:
    - ReadWriteOnce
  storageClassName: manual
  